import os, sys
import subprocess
from stat import *
import stat
import os.path


def pendrive:

    os.path.exists("/dev/sdb")

while
    print('PenDrive sendo formatado!')
    subprocess.call(["mkfs.vfat /dev/sdb"], shell = True)
